# Shared dotfiles module
# This module is a placeholder for future dotfiles functionality
# Currently, dotfiles are managed at the flake level via specialArgs

{ config, lib, ... }: {
  # Enable dotfiles support
  # (dotfiles are provided via dotfilesDir specialArg from flake.nix)

  # Future: Add dotfiles-specific options and configuration here
  # For now, this module exists for consistency and future extensibility
}
